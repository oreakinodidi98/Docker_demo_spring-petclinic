# This file is no longer needed for Azure PostgreSQL Flexible Server
# The database connection is now managed by Azure Service Connector
# with passwordless authentication using Entra ID and Workload Identity
#
# Connection details are stored in the Kubernetes secret: sc-pg-secret
# Service Account: sc-account-acda3ed9-e3d0-4f77-8887-37e2db05f5df
#
# To view the connection details:
#   kubectl get secret sc-pg-secret -o yaml
#
# Azure PostgreSQL Flexible Server details:
#   Server: db-petclinic189769.postgres.database.azure.com
#   Database: petclinic
#   Authentication: Entra ID (Managed Identity)
#   Username: aad_pg
